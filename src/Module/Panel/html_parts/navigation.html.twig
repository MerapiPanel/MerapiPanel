<header class="max-w-[1440px] mx-auto">
    <div class="w-full flex items-baseline">
        <a>
            <span class="font-bold text-2xl">Merapi Panel</span>
        </a>

        <ul class="ms-auto flex">
            {% for item in guest.panel_navs %}
            <li><a href="{{ item.link }}">{{ item.name }}</a></li>
            {% endfor %}
        </ul>




    </div>
</header>