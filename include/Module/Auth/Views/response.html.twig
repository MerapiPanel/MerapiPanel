{% extends 'base.html.twig' %}

{% block content %}

<div class="container">
    
<div class="d-flex flex-column vh-100 ">


    <section class="text-center flex-grow-1 d-flex flex-column justify-content-center">
        <header class="mb-4">
            <i class="text-{{ status }} fa-solid fa-triangle-exclamation fa-5x"></i>
        </header>
        <p class="fs-3 text-{{ status }} mb-5 w-100" style="max-width: 600px; margin: 0 auto;">{{ message }}</p>
        <p>redirect in <b id="countDown">%s</b> seconds or click <a class="d-inline-block px-1" href="{{ redirect }}">here</a> to redirect</p>
    </section>

    <footer class="mt-auto py-4 w-100">
        <div class="row">
            <div class="col-12 col-lg-6 d-flex justify-content-start">
                <div class="border-end d-inline-block mb-2 me-1 pe-3">
                    <img width="90" height="40" src="{{ 'assets/img/logo.png' | assets }}" alt="">
                </div>
                <div class="mt-3">
                    <a class="btn btn-link" href="https://www.npmjs.com/package/@il4mb/merapipanel">
                        <i class="fa-brands fa-npm fa-xl"></i>
                    </a>
                    <a class="btn btn-link" href="https://github.com/MerapiPanel/MerapiPanel" target="_blank">
                        <i class="fa-brands fa-github fa-xl"></i>
                    </a>
                </div>
            </div>
        </div>
        <div class="text-center">Copyright © {{ 'now' | date('Y') }} Merapi. All rights reserved.</div>
    </footer>
</div>

</div>


{% endblock %}

{% block javascript %}
<script>
    var countDown = 5;

    setInterval(function () {
        if (countDown > 0) {
            document.querySelector('#countDown').innerHTML = countDown;
            countDown--;
        } else {
            window.location.href = "{{ redirect }}";
        }
    }, 1000);
</script>
{% endblock %}